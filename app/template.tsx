import React from "react";

const Template = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            {/* template */}
            { children }
        </div>
    )
}

export default Template;